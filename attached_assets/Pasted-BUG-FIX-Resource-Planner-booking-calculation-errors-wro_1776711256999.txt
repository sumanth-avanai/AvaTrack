BUG FIX: Resource Planner booking calculation errors – wrong values and timing issues.

⚠️ IMPORTANT: Preserve ALL existing data. No destructive changes.

=== BUG DESCRIPTION ===

When editing a booking:
- Start: 20.04.2026, End: 29.06.2026
- Allocation: 40h/week (= 5 days/week)
- "Working days in period: 51d"
- "Bookable days: 51d"
- "This booking: 55d" ← WRONG (exceeds bookable days, impossible)
- User originally entered 50 days, end date was auto-calculated

PROBLEMS:
1. "This booking: 55d" is impossible – cannot exceed bookable days (51d)
2. End date auto-calculation off by 1 (should give exactly 50 working days, not 51)
3. Values might be stale (calculated once, not updating live)

=== ROOT CAUSE INVESTIGATION ===

Check for:
1. OFF-BY-ONE ERROR: Is end date inclusive or exclusive in calculation?
2. STALE STATE: Are values recalculating when form fields change?
3. SAVED VS. LIVE: Is modal showing old saved values instead of recalculating?
4. RACE CONDITION: Multiple async calculations overwriting each other?

Debug by logging:
- Inputs: startDate, endDate, hoursPerWeek
- Outputs: workingDays, bookableDays, thisBookingDays
- Check if inputs match displayed form values

=== CORRECT LOGIC ===

1. WORKING DAYS IN PERIOD:
   - Count Mo-Fr between start and end (inclusive)
   - MINUS holidays
   - MINUS employee vacation days

2. THIS BOOKING (days):
   - If full-time (40h/week): equals working days in period
   - If part-time: (hoursPerWeek / 40) × working days
   - MUST NEVER exceed bookable days

3. END DATE AUTO-CALCULATION:
   - When user enters "50 total days" + start date
   - Find the date where exactly 50 working days have passed
   - Result should give bookable days = 50 (not 51)

=== CORRECT BEHAVIOR ===

1. LIVE RECALCULATION on every change:
   - User changes Start Date → recalculate
   - User changes End Date → recalculate
   - User changes Allocation → recalculate
   - No stale values

2. STATE MANAGEMENT:
```javascript
   useEffect(() => {
     if (startDate && endDate && hoursPerWeek) {
       calculateBookingSummary({
         employeeId,
         startDate,
         endDate,
         hoursPerWeek,
         projectRoleId
       }).then(setBookingSummary);
     }
   }, [startDate, endDate, hoursPerWeek, employeeId, projectRoleId]);
CONSISTENT VALUES:
If bookable days = 51, then this booking ≤ 51
All three numbers must make mathematical sense
=== FIX CHECKLIST ===

Fix off-by-one in end date auto-calculation
Fix "This booking" to never exceed bookable days
Ensure live recalculation on every input change
Use current form values, not stale/saved values
Add proper useEffect dependencies
Test: change dates → values update immediately
Preserve all existing bookings and data.


