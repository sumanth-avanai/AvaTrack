Sieht schon gut aus! Hier der Prompt für die Erweiterungen:

---

**Enhancement: Admin Timesheet – Extended Filters + Bulk Edit**

**⚠️ IMPORTANT: Preserve ALL existing data and functionality. Only extend the existing AdminTimesheetView.**

**1. Extended Date Filter**

Replace the current Month picker with a more flexible date range control:

```
[All time ▼]  or  [Custom: 01.06.2026 — 30.06.2026]
```

Options in dropdown: `This month` (default) · `Last month` · `This year` · `All time` · `Custom range`  
Custom range: two date inputs (from / to).  
When "All time" is selected, fetch without date filters.

**2. Client Filter**

Add a "Client" dropdown between Employee and Project filters:
- Populate from all distinct clients across projects
- Selecting a client filters the Project dropdown to only that client's projects
- Order: Month | Employee | **Client** | Project | No role only

**3. Bulk Edit**

Add a checkbox column as the first column. Behavior:

```
☑  DATE        EMPLOYEE         PROJECT / ROLE           HOURS
☑  05.06.2026  Abdelrahman...   n8n extension / ...       8h   ✏️ 🗑️
☐  03.06.2026  Abdelrahman...   n8n extension / ...       8h   ✏️ 🗑️
```

- Checkbox in header = select/deselect all (current page)
- When ≥ 2 rows selected → show bulk action bar above table:

```
┌─────────────────────────────────────────────────────────┐
│  3 entries selected   [Change Role ▼]   [Apply]  [Cancel]│
└─────────────────────────────────────────────────────────┘
```

Bulk actions available:
- **Change Role**: dropdown with Project (first) → Role (filtered by project); applies `PATCH /api/time-entries/:id` for each selected entry
- **Change Project**: same pattern

On Apply: show confirmation "Update 3 entries?" → confirm → PATCH all selected → invalidate cache → deselect all → show success toast.

**Acceptance Criteria:**
- [ ] Date filter supports All time, This month, Last month, This year, Custom range
- [ ] Client filter populates from project.client; filters project dropdown
- [ ] Selecting 2+ rows shows bulk action bar; single-row edits still work per existing pencil icon
- [ ] Bulk role/project change PATCHes all selected entries and shows success/error toast
- [ ] No changes to employee timesheet view